import 'package:flutter/material.dart';
import 'body.dart';

//Connected Central Devices

class Connected_CentDev extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
